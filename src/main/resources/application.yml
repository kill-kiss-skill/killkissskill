spring:
  application:
    name: itas-backend
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

server:
  port: 8080
  servlet:
    context-path: /

knife4j:
  enable: true
  setting:
    language: zh_cn
